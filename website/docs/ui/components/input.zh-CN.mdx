---
title: 输入框
description: 输入组件是用于在文本字段中获取用户输入的组件。
---

## 导入

```javascript
import { Input, InputGroup, InputAddon } from '@resolid/nxt-ui';
```

## 用法

<Demo file={'InputDemo.tsx'} height={302} demo={'Usage'} />

## 大小

输入框有五种可供使用的尺寸。 通过传递 `size` 属性，可以选择使用 `xs`、`sm`、`md`、`lg` 或 `xl` 中的任意一种。

<Demo file={'InputDemo.tsx'} height={231} demo={'Sizes'} />

## 可控

<Demo file={'InputDemo.tsx'} height={333} demo={'Controlled'} />

## HTML 表单

<Demo file={'InputDemo.tsx'} height={567} demo={'HtmlForms'} />

## 前缀和后缀

<Demo file={'InputDemo.tsx'} height={423} demo={'PrefixSuffix'} />

## 输入框组

<Demo file={'InputDemo.tsx'} height={1247} demo={'Group'} />

## 属性

<PropsTable title component={'Input'} />
<PropsTable title component={'InputGroup'} />
